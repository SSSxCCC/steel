{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/steel-editor.exe",
            "cwd": "${workspaceRoot}",
            "console": "integratedTerminal",
            "preLaunchTask": "cargo build"
        }
    ]
}